name: Update Multi-source Rules

on:
  workflow_dispatch:
  schedule:
    - cron: "17 16 * * *"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/update-rules.yml"
      - ".github/scripts/update_rules.py"
      - ".github/scripts/test_update_rules.py"
  pull_request:
    paths:
      - ".github/workflows/update-rules.yml"
      - ".github/scripts/update_rules.py"
      - ".github/scripts/test_update_rules.py"
      - ".github/rule-source-snapshot.json"
      - "Rules/**"

permissions:
  contents: read

env:
  PYTHONDONTWRITEBYTECODE: "1"

concurrency:
  group: update-multisource-rules
  cancel-in-progress: false

jobs:
  validate:
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"

      - name: Test Surge rule parser
        run: python .github/scripts/test_update_rules.py

      - name: Generate and validate multi-source rules
        run: python .github/scripts/update_rules.py

      - name: Verify generated files are committed
        run: |
          git diff --exit-code -- Rules/*.list .github/rule-source-snapshot.json
          test -z "$(git status --porcelain --untracked-files=all)"

  update:
    if: github.event_name != 'pull_request'
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"

      - name: Test Surge rule parser
        run: python .github/scripts/test_update_rules.py

      - name: Generate and validate multi-source rules
        run: python .github/scripts/update_rules.py

      - name: Commit changes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Rules/*.list .github/rule-source-snapshot.json
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "update multi-source rules"
            git push
          fi

  notify:
    needs: update
    if: always() && github.event_name == 'schedule'
    permissions:
      contents: read
      issues: write
    runs-on: ubuntu-latest
    steps:
      - name: Report scheduled update status
        env:
          GH_TOKEN: ${{ github.token }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          UPDATE_RESULT: ${{ needs.update.result }}
        shell: bash
        run: |
          set -euo pipefail
          title="Scheduled Surge rule update failed"
          issue_url="$(gh issue list --repo "$GITHUB_REPOSITORY" --state open --search "$title in:title" --json title,url --jq '.[] | select(.title == "Scheduled Surge rule update failed") | .url' | head -n 1)"
          if [ "$UPDATE_RESULT" = "failure" ]; then
            body="The scheduled rule update failed. Run: $RUN_URL"
            if [ -n "$issue_url" ]; then
              gh issue comment "$issue_url" --body "$body"
            else
              gh issue create --repo "$GITHUB_REPOSITORY" --title "$title" --body "$body"
            fi
          elif [ -n "$issue_url" ]; then
            gh issue comment "$issue_url" --body "The scheduled rule update recovered. Run: $RUN_URL"
            gh issue close "$issue_url"
          fi
