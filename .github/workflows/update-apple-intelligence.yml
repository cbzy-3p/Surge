name: Update Apple Intelligence Rules

on:
  workflow_dispatch:

permissions:
  contents: write

# Compatibility entry point: use the same validated pipeline as scheduled updates.
jobs:
  update:
    uses: ./.github/workflows/update-rules.yml
